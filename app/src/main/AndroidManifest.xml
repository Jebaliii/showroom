<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:enableOnBackInvokedCallback="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Showroom"
        tools:targetApi="31">

        <!-- Your existing activities -->
        <activity
            android:name=".Payement"
            android:exported="false" />
        <activity
            android:name=".Detailvoiture"
            android:exported="false" />
        <activity
            android:name=".Listevoitures"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- New activities for Admin, addFragment, updateFragment, deleteFragment -->
        <activity
            android:name=".Admin"
            android:exported="false" />
        <activity
            android:name=".add"
            android:exported="false" />
        <activity
            android:name=".update"
            android:exported="false" />
        <activity
            android:name=".deleteFragment"
            android:exported="false" />
    </application>


</manifest>